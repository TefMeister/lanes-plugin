<!-- One file per project. Edit only the file for the project you are working on,
     and stage only that path, so two concurrent sessions can never touch the
     same file. Delete this example once you have a real one. -->

## Example Project

OPEN (2026-01-01):
  [PD]        a step that needs nothing running
  [USER]      a step that needs a person, not the app
  [FLAT]      a step that needs the app running
  [VR CLAUDE] a step that needs the hardware connected, but nobody using it
  [VR USER]   a step that needs a person actually using the hardware

- **2026-01-01 - what happened, and what it means.** The log goes here, newest
  first. The OPEN block above says what each REMAINING step requires; this log
  says what already happened. Long-tail ideas live down here, never in OPEN -
  if the wishlist gets enumerated up there, the [PD] bucket never empties and
  the board becomes decoration.
  - Tag durable claims with how well they are known `[hypothesis]`, and remember
    that `n=1` is not verified.
